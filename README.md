# Drink Bot - Telegram бот для поиска компании

Telegram-бот для поиска компании для встреч и тусовок, созданный с использованием aiogram 3.

## Функционал

- 📝 Создание и редактирование анкет
- 🔍 Поиск собутыльников по городу
- 🍺 Хранение информации о любимых напитках
- 🏙️ Фильтрация по городам

## Структура проекта

```
Drink bot/
├── main.py              # Основной файл запуска бота
├── handlers/
│   └── start.py         # Обработчики команд и сообщений
├── database/
│   └── models.py        # Модели и работа с базой данных SQLite
├── requirements.txt     # Зависимости Python
├── .env.example         # Пример файла с переменными окружения
└── README.md           # Этот файл
```

## Установка и запуск

### 1. Клонируйте или скачайте проект

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

### 3. Создайте токен бота

1. Найдите в Telegram @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен

### 4. Настройте переменные окружения

Скопируйте файл `.env.example` в `.env`:

```bash
cp .env.example .env
```

Откройте файл `.env` и вставьте ваш токен:

```
BOT_TOKEN=ваш_токен_здесь
```

### 5. Запустите бота

```bash
python main.py
```

## Использование

1. Запустите бота командой `/start`
2. Нажмите "📝 Заполнить анкету" для создания профиля
3. Следуйте инструкциям для ввода данных (имя, возраст, город, любимый напиток)
4. Используйте "🔍 Найти собутыльника" для поиска людей в вашем городе

## База данных

Бот использует SQLite базу данных, которая автоматически создается при первом запуске в файле `drink_bot.db`.

## Технологии

- **Python 3.8+**
- **aiogram 3.4.1** - асинхронная библиотека для Telegram Bot API
- **SQLite** - встроенная база данных
- **python-dotenv** - управление переменными окружения

## Команды бота

- `/start` - Приветствие и главное меню
- "📝 Заполнить анкету" - Создание или обновление анкеты
- "🔍 Найти собутыльника" - Поиск людей в вашем городе
