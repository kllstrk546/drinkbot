# GitHub Deployment Guide for Drink Bot

## 🚀 ЗАГРУЗКА НА GITHUB

### 📋 ЧТО СДЕЛАНО:

#### ✅ **СОХРАНЕНИЕ ДАННЫХ:**
- **✅ Бэкап базы данных:** `drink_bot_backup_2026-01-30_16-05-26.db`
- **✅ SQL дамп:** `drink_bot_backup_2026-01-30_16-05-26.sql`
- **✅ .gitignore:** Настроен для исключения чувствительных файлов
- **✅ Авто-бэкап скрипт:** `auto_backup.sh`

#### ✅ **ИСПРАВЛЕННЫЕ ПРОБЛЕМЫ:**
- **✅ Кнопка "Назад"** - возвращает в главное меню
- **✅ Дубликаты анкет** - устранены
- **✅ Лимиты ботов** - работают по городам
- **✅ Ежедневная ротация** - автоматическая
- **✅ Уведомления** - неактивным пользователям
- **✅ Import datetime** - исправлен

---

### 📁 СТРУКТУРА ФАЙЛОВ:

```
📁 Drink bot/
├── 📄 main.py                    # Основной файл бота
├── 📄 handlers/start.py          # Все хэндлеры
├── 📄 database/models.py         # Модели базы данных
├── 📄 locales.py                 # Переводы
├── 📄 .env                       # Переменные окружения
├── 📄 .gitignore                 # Исключения для Git
├── 📄 auto_backup.sh             # Авто-бэкап
├── 📄 drink_bot.db               # База данных (не загружать!)
├── 📄 drink_bot_backup_*.db      # Бэкапы
└── 📁 ...другие файлы
```

---

### 🛠️ ИНСТРУКЦИЯ ПО ЗАГРУЗКЕ:

#### **ШАГ 1: Создание репозитория**
1. Зайдите на [GitHub](https://github.com)
2. Нажмите "New repository"
3. Название: `drink-bot`
4. Описание: `Telegram Dating Bot with Daily Rotation`
5. Private/Public: выберите
6. Не добавляйте README, .gitignore, license
7. Нажмите "Create repository"

#### **ШАГ 2: Клонирование локально**
```bash
git clone https://github.com/ВАШ_НИК/drink-bot.git
cd drink-bot
```

#### **ШАГ 3: Копирование файлов**
```bash
# Скопируйте все файлы из текущей папки в папку drink-bot
# НЕ копируйте drink_bot.db!
```

#### **ШАГ 4: Первая загрузка**
```bash
git add .
git commit -m "Initial commit - Drink Bot with all features"
git push -u origin main
```

---

### ⚙️ НАСТРОЙКА АВТОМАТИЧЕСКИХ БЭКАПОВ:

#### **GitHub Actions:**
1. Создайте папку `.github/workflows`
2. Добавьте файл `backup.yml`:

```yaml
name: Automatic Database Backup
on:
  schedule:
    - cron: '0 */6 * * *'
jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    - name: Install dependencies
      run: pip install -r requirements.txt
    - name: Backup database
      run: python backup_all_data.py
    - name: Upload to GitHub
      uses: actions/upload-artifact@v3
      with:
        path: drink_bot_backup_*.db
        name: database-backup
        retention-days: 30
```

#### **Локальные бэкапы:**
```bash
# Сделайте auto_backup.sh исполняемым
chmod +x auto_backup.sh

# Добавьте в crontab для бэкапов каждые 6 часов
crontab -e
# Добавьте строку:
0 */6 * * * /path/to/drink-bot/auto_backup.sh
```

---

### 🚀 РАЗВЕРТЫВАНИЕ НА СЕРВЕРЕ:

#### **Требования:**
- Python 3.9+
- SQLite3
- aiogram
- python-dotenv
- geopy

#### **Установка:**
```bash
# Клонируйте репозиторий
git clone https://github.com/ВАШ_НИК/drink-bot.git
cd drink-bot

# Установите зависимости
pip install -r requirements.txt

# Настройте .env файл
cp .env.example .env
# Добавьте BOT_TOKEN в .env

# Запустите бота
python main.py
```

---

### 📊 СИСТЕМА ЛИМИТОВ:

#### **Лимиты по городам:**
- **Tier 1 (Kyiv, Moscow):** 15 анкет в день
- **Tier 2 (крупные):** 10 анкет в день
- **Tier 3 (средние):** 8 анкет в день
- **Tier 4 (маленькие):** 5 анкет в день

#### **Ежедневная ротация:**
- **Автоматическая** в полночь
- **1002 бота** перемешиваются
- **Сбалансированное** распределение по гендеру

---

### 🔄 ВОССТАНОВЛЕНИЕ ДАННЫХ:

#### **Из бэкапа:**
```bash
# Остановите бота
# Замените drink_bot.db на бэкап
cp drink_bot_backup_2026-01-30_16-05-26.db drink_bot.db
# Запустите бота
python main.py
```

#### **Из SQL дампа:**
```bash
sqlite3 drink_bot.db < drink_bot_backup_2026-01-30_16-05-26.sql
```

---

### 🎯 КЛЮЧЕВЫЕ ФИЧИ:

#### ✅ **Реализовано:**
- **🔄 Ежедневная ротация ботов**
- **📊 Лимиты по городам**
- **⬅️ Кнопка "Назад"**
- **📬 Уведомления неактивным**
- **👥 Баланс гендеров**
- **🏙️ Поиск по городам**
- **💾 Авто-бэкапы**

#### ⚠️ **Важно:**
- **НЕ загружайте** `drink_bot.db` в GitHub
- **ВСЕГДА делайте бэкап** перед изменениями
- **Проверьте .env** на сервере
- **Мониторьте логи** бота

---

### 📞 ПОДДЕРЖКА:

#### **Если проблемы:**
1. **Проверьте логи** бота
2. **Проверьте .env** файл
3. **Проверьте права** доступа
4. **Восстановите из бэкапа**

#### **Полезные команды:**
```bash
# Проверить статус бота
ps aux | grep python

# Перезапустить бота
pkill -f python && python main.py

# Проверить базу данных
sqlite3 drink_bot.db ".tables"
```

---

### 🎉 ГОТОВО!

**✅ Все данные сохранены**
**✅ Бэкап создан**
**✅ GitHub готов**
**✅ Инструкции предоставлены**

**Теперь можно загружать на GitHub и разворачивать на сервере!** 🚀
